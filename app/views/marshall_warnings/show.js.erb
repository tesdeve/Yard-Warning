// Eliminates spaces at the top
$('br').remove()

// Removes the index table
$("#indexwarnings").remove()

// Removes the index table from Search
$("#indexSearch").remove()

// Presents the Product
$('#allViews').append('<%= j render(@marshall_warning) %>')