//alert("INDEX!!")

// From Show view
$('br').remove()
$("#showProduct").remove()

// Removes any form
$("#primerForm").remove()
$(".updateForm").remove()

// Removes the index table from search
$("#indexSearch").remove()

// Removes the index table from History
$("#indexwarningsHistory").remove()

$('#allViews').append('<%= j render("index") %>')

// Replace and reload the index page
// Great article =>   https://dev.to/samanthaming/window-location-cheatsheet-4edl

function reloadRootPage() {
  let origin = window.location.origin
  if (origin === "http://localhost:3000"){
  	location.replace('http://localhost:3000');
	} else if (origin === 'https://ywarnings.herokuapp.com'){
		location.replace('https://ywarnings.herokuapp.com');
	} else if (origin === 'http://ywarnings.herokuapp.com'){
		location.replace('http://ywarnings.herokuapp.com');
	}
}
reloadRootPage()


















